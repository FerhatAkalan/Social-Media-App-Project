/// <reference types="react" />
import { EmojiStyle } from '../../types/exposedTypes';
import { GetEmojiUrl } from './Emoji';
export declare function ExportedEmoji({ unified, size, emojiStyle, lazyLoad, getEmojiUrl, }: {
    unified: string;
    emojiStyle?: EmojiStyle;
    size?: number;
    lazyLoad?: boolean;
    getEmojiUrl?: GetEmojiUrl;
}): JSX.Element | null;
